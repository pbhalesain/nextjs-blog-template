
'use client'
type PageHeadProps = {
	title: string;
	description: string;
};

const PageHead: React.FC<PageHeadProps> = ({ title, description }) => {
	return (
		<div>abcd</div>
	);
};

export default PageHead;